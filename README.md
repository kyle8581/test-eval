# Instruction-following audit (Prolific study page)

Static jsPsych 8 study page. Responses are saved through DataPipe to OSF. Settings live in `config.js`.
Open `index.html?debug=1&list=0` to try it without saving any data.
